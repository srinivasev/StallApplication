apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: np-for-postgress
  namespace: default
spec:
  podSelector:
    matchLabels:
      appType: backend
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 6379
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy 
metadata:
  name: np-for-postgress
spec:
  podSelector:
    matchLabels:
      appType: backend
  ingress:
  - from:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5432